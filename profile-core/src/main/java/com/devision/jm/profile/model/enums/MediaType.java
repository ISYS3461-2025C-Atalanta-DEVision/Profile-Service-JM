package com.devision.jm.profile.model.enums;

/**
 * Media Type Enum
 *
 * Represents the type of media in the company gallery.
 *
 * Implements requirements:
 * - 3.2.2: Company Gallery (images and videos)
 */
public enum MediaType {
    IMAGE,
    VIDEO
}
