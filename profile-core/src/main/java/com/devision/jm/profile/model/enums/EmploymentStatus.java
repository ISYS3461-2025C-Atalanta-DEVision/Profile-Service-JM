package com.devision.jm.profile.model.enums;

/**
 * Employment Status Enum
 *
 * Represents the desired employment status for applicant searching.
 *
 * Implements requirements:
 * - 6.2.2: Desired Employment Status (multiple selection)
 */
public enum EmploymentStatus {
    FULL_TIME,
    PART_TIME,
    CONTRACT,
    FREELANCE,
    INTERNSHIP,
    REMOTE,
    ON_SITE,
    HYBRID
}
